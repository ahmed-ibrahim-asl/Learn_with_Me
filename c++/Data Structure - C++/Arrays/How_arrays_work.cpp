#include <iostream>
using namespace std;

int main(){

/*     int size;
    cin >> size;
    float months[size], total;


    // inserting elements in the array 
    for(int i =0; i<=size; i++){
        cout << "Enter element num " << i+1 << ": ";
        cin >> months[i];
        total += months[i];
    }


    cout << "total " << total << endl;
    cout << "average " << total/(size*1) << endl;
    cout << "average in Two years: " << total/(size*2); */

    // Trying to set the size of the array in runtime of the program
    int size;
    cout << "Size of the array: ";
    cin >> size;
    float monthsArray[size];
    


    /*     
                    Summary:
    
    




    */

    

}
